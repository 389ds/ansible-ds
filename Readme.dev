

To build the ansible galaxy collection (in ansible_collections/ds-ansible_ds-*.tar.gz):
	make build

To install it:
    ansible-galaxy collection install  path_to_ds-ansible_ds-*.tar.gz -f

To run a playbook:
    ansible-playbook ds.ansible_ds.gather_dsinst_info.yml
